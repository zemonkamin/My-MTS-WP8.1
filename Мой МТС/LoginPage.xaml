<phone:PhoneApplicationPage
    x:Class="Мой_МТС.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    SupportedOrientations="Portrait" Orientation="Portrait"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    Foreground="White"
    shell:SystemTray.IsVisible="True"
    shell:SystemTray.BackgroundColor="Black"
    shell:SystemTray.ForegroundColor="White">

    <Grid Background="Black">
        <ScrollViewer>
            <StackPanel Margin="24,38,24,24">
                <TextBlock Text="Мой МТС" FontSize="46" FontWeight="SemiBold"/>
                <TextBlock Text="вход в аккаунт" FontSize="28" Foreground="#FF9AA0A6" Margin="0,4,0,36"/>


                    <StackPanel>
                        <TextBlock Text="Номер телефона" FontSize="22" Foreground="#FF9AA0A6"/>
                        <Grid Margin="0,12,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="92"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" BorderBrush="White" BorderThickness="1" Background="Transparent" MinHeight="72" Margin="0,10">
                                <TextBlock Text="+7" FontSize="30" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>

                            <TextBox x:Name="PhoneTailBox"
                                     Grid.Column="2"
                                     FontSize="30"
                                     MaxLength="10"
                                     InputScope="TelephoneNumber"/>
                        </Grid>

                    </StackPanel>


                <Button x:Name="SmsButton" Content="Получить SMS-код" Click="SmsButton_Click"/>
                <TextBlock x:Name="StatusText" TextWrapping="Wrap" FontSize="22" Foreground="#FF9AA0A6" Margin="0,20,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</phone:PhoneApplicationPage>
